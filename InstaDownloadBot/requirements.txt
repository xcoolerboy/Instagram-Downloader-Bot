# اکسترای [job-queue] برای کارهای دوره‌ای (پاک‌سازیِ کش/فایل موقت + یادآوریِ انقضای VIP).
# اگه روی هاست نصب نشد، ربات بدونِ کارهای زمان‌بندی‌شده باز هم سالم بالا میاد (bot.py گارد داره).
python-telegram-bot[job-queue]==21.6
yt-dlp>=2024.10.7
instaloader>=4.13.1
aiosqlite>=0.20.0
python-dotenv>=1.0.1
requests>=2.32.3
# باینریِ آماده‌ی ffmpeg برای ساختِ «نسخه‌ی کم‌حجم» ریلز (بدونِ نیاز به نصبِ سیستمی)
imageio-ffmpeg>=0.5.1
# شناساییِ نامِ آهنگِ ریلز برای دکمه‌ی «🔎 این آهنگ چیه؟» (با numpy/requests، بدونِ سرویسِ پولی).
# ✅ بی‌خطر برای رباتای دیگه: ShazamAPI فقط به numpy/requests/pydub وابسته‌ست و «هیچ» ربطی
#    به pydantic نداره — پس برخلافِ shazamio، نصبش محیطِ مشترک و باتای aiogram رو نمی‌شکنه.
#    (روی پایتونِ ۳.۹ سرور کار می‌کنه؛ روی ۳.۱۳+ چون audioop حذف شده دکمه خودکار مخفی می‌شه.)
ShazamAPI==0.0.2
